class NetworkFactory:

    def CreateNetwork(networkName):
        network = None

        if networkName == "LSTM"    :
            return network
        elif networkName == "CNN"   :
            return network
